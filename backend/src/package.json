{
  "name": "capyweb-backend",
  "private": true,
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.1141.0",
    "@aws-sdk/client-dynamodb": "^3.1141.0",
    "@aws-sdk/client-ssm": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.1141.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types . lib/"
  }
}
