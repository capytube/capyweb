# Secrets known to be in this repository's git history.
#
# Every entry here is PUBLIC and must be treated as burned: rewriting history does not
# recall a clone that already exists. The only real remedy is rotation.
#
# Identifiers are redacted (first 6 and last 4 characters). They identify a secret without
# being usable. Never put a full secret in this file.
#
# Regenerate the identifiers with:  scripts/scan-history.py
# Gate on "no NEW findings" with:   scripts/scan-history.py --known-ok
#
# ---------------------------------------------------------------------------------------
# AppSync API keys (6), all from amplify_outputs.json, committed before it was gitignored.
# STATUS: dead. All the AppSync APIs they belonged to are deleted - every historic endpoint
# resolves NXDOMAIN (verified 2026-09-26, with controls). No rotation possible or needed.
# Context: capyweb-py9.
appsync-api-key:da2-f3…znqq
appsync-api-key:da2-jh…unji
appsync-api-key:da2-kx…av3e
appsync-api-key:da2-na…4sim
appsync-api-key:da2-nu…r7ga
appsync-api-key:da2-q7…z7gu

# ---------------------------------------------------------------------------------------
# Livepeer API keys (2 distinct values, across 3 files: amplify/functions/getStream,
# amplify/functions/getViewership, and the now-deleted src/utils/livepeerService.ts).
# STATUS: LIVE AND UNROTATED. These still work. Rotating them is capyweb-962 and needs
# nic's Livepeer Studio sign-in. Once rotated, write the new key to SSM
# /capyapp/capyweb/<stage>/livepeer-api-key and update this note.
credential-assignment:3e2c0d…8661
credential-assignment:7f0954…9b62
